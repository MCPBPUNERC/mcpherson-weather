# Optional: private KWCH JSON endpoint. If omitted, the app will use NWS fallback automatically.
KWCH_WEATHER_JSON_URL=https://your-private-kwch-endpoint.example.com/weather

# NOAA recommends a contact in the User-Agent header.
NWS_USER_AGENT="McPhersonWeatherLink (your-email@example.com)"
